name: Deploy Studio to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - pages-site/**
      - src/**
      - scripts/build-pages.mjs
      - scripts/verify-pages-demo.ts
      - package.json
      - package-lock.json
      - public/assets/brand/logo.png
      - public/assets/library/stadium.png
      - public/assets/library/arena.png
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install demo browser
        run: npx playwright install --with-deps chromium

      - name: Build static Studio
        run: npm run build:pages

      - name: Verify read-only demo
        run: npm run test:pages-demo

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages-site/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
